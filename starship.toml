# format = "$all"
format = """$directory$git_branch$git_commit$git_status$cmd_duration
$package$dotnet$kubernetes$golang$nodejs$python$ruby$swift
$hostname$character"""

# Utilities

[directory]
truncate_to_repo = true
style = "blue"
truncation_symbol = "…/"

[character]
success_symbol = "[❯](bright-green)"
error_symbol = "[❯](bright-red)"

[package]
format = "[$symbol$version]($style) "
style = "208"

[hostname]
format = "[$hostname]($style)"

[cmd_duration]
style = "yellow"
format = "[$duration]($style) "
show_notifications = true

# Git

[git_branch]
format = "[$branch]($style) "
style = "purple"

[git_commit]
style = "203"
format = "[$hash]($style) [$tag]($style) "
commit_hash_length = 8
tag_symbol = "🏷  "
only_detached = false
tag_disabled = false

[git_status]
format = "([$all_status$ahead_behind]($style) )"
style = "cyan"

# Languages

[golang]
style = "cyan"
format = "[$symbol($version )]($style) "
symbol = "ﳑ "

[nodejs]
style = "green"
format = "[$symbol$version]($style) "

[python]
format = '[${symbol}${pyenv_prefix}${version}( \($virtualenv\))]($style) '
style = "yellow"
pyenv_version_name = true
symbol = " "

[ruby]
style = "red"
format = "[$symbol($version )]($style) "
symbol = " "

[swift]
style = "202"
symbol = " "

# Tools
[kubernetes]
disabled = false
