[user]
	# needs to match GitHub name for signing to work in Codespaces.
	# GitHub doesn't have a web-flow signing SSH key yet, so GPG is necessary
	# in Codespaces.
    name = "Johanan"
    email = "jidicula@github.com"
    # signingkey = "D97377F4"
	signingkey = "~/Library/Containers/com.maxgoedjen.Secretive.SecretAgent/Data/PublicKeys/14b2f8686247bedac8b118d47b2af32f.pub"
[gpg]
	format = "ssh"
[gpg "ssh"]
	allowedSignersFile = "~/dotfiles/allowed-signers"
